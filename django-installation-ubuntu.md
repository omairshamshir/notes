```
    sudo apt-get update
    sudo apt-get upgrade
    sudo apt-get dist-upgrade
    sudo apt-get install build-essential
    sudo apt-get install acl
    sudo apt-get install python-dev zlib1g-dev libxml2-dev libxslt1-dev libssl-dev git curl python3-pip
    pip3 install virtualenv
    cd {required_destination}
    virtualenv library
    source library/bin/activate
    pip3 install django

```
